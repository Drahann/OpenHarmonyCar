# Plants E-commerce iOS Figma Extraction

Static implementation for Figma file `KffDJpXDr7fKvyWDcN0rxE`.

- `index.html` implements the `Plants-1` multi-screen showcase from node `1:57`.
- `fonts-colors.html` implements the style board from node `1:267`.
- `assets/` contains downloaded plant image assets from Figma MCP.
- `references/` contains Figma reference screenshots.

The original Satoshi font is referenced by name in CSS with system fallbacks; no font package or runtime dependency is required.
