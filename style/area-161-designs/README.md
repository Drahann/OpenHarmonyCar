# Area Figma Designs 161

Static implementation of four Figma nodes from file `65CMrCi7opIqi80NPrKFxu`.

- `index.html` implements Home, node `161:721`.
- `hero-image.html` implements Hero image, node `161:251`.
- `thumbnail.html` implements Thumbnail, node `161:871`.
- `style-guide.html` implements Styles, node `161:722`.
- `assets/` contains downloaded Figma assets so the implementation does not rely on short-lived Figma MCP URLs.
- `references/` contains reference screenshots captured from Figma for visual comparison.

Open any HTML file directly in a browser. No build step or dependency install is required.
