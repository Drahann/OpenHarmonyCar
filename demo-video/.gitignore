# 构建产物：film.html 由 build.sh 从 film.css + engine.js + scenes*.js + vendor 组装生成，
# 可一键复现（bash build.sh），按 CLAUDE.md「不提交构建产物」忽略，只版本化源文件。
film.html

# 依赖与生成目录（根 .gitignore 已覆盖 node_modules，此处显式重申）
node_modules/

# 逐帧导出的图片产物（如有）
stills/*.png
stills/*.jpg

# 校验脚本的临时截图（shot.mjs 输出）
_tmpframes/

# 资源素材：只版本化演示片实际引用的三个素材（meter.mp4 / pi-board.png / monitor.png），
# 其余原始截图、参考图与 ASCII/中文同名重复副本留作本地工作件，不进 git
# （遵 CLAUDE.md「不提交大文件、大段录制视频走外部存储」）。
assets/*
!assets/meter.mp4
!assets/pi-board.png
!assets/monitor.png
