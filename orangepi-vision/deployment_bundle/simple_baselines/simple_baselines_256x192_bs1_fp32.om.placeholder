# 占位符 —— 实际权重未纳入 git

此处应放 Simple Baselines 关键点模型（昇腾 NPU OM，4 关键点，FP32）。
权重本体**不进 git**（见根 `.gitignore`；账号 Git LFS 额度已用尽，暂不走 LFS）。

| 字段 | 值 |
|---|---|
| 文件名 | `simple_baselines_256x192_bs1_fp32.om` |
| 大小 | 68819005 bytes (≈65.6 MB) |
| sha256 | `58d5551036ff2248ce018e268a00844ec458e784317b51b600937ae0bb31f573` |
| 来源 | 香橙派 `~/simple_baselines/deployment_bundle/simple_baselines/simple_baselines_256x192_bs1_fp32.om` |
| 输入 | `[1,3,256,192]` NCHW，ImageNet 归一化；输出 heatmap `[1,4,64,48]` |
| 关键点顺序 | `0=pointer_tip, 1=center, 2=zero, 3=full`（见 README 对接提醒） |

## 获取方式
从香橙派（`192.168.1.5`，用户 `HwHiAiUser`）拷贝。
放回本目录、文件名与上表一致即可运行 `../../test_gauge_reading_multi.py`。
