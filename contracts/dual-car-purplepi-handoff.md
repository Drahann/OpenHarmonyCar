# 紫派交接 · 方案A「平板直连双车」架构变更 → 成员A

> **致**：成员A（紫派 / OpenHarmony，`origin/purplepi-control`）。
> **日期**：2026-06-13。**性质**：架构变更通知 + 少量确认项（CLAUDE.md：跨设备/架构变更需另一端同步）。
> **配套**：App 侧实施单 `docs/dual-car-plan.md`；协议表 `contracts/udp-protocol.md`（命令布局未变）。

---

## TL;DR（一句话）

**多机协同从「软总线 DDO + 车载 agent」改成「平板直连两辆车的 UDP」。对你来说：C/C++ 几乎不用改**——
你已实现并经 Q13 对齐的那套命令（cmd105 拉图 / cmd5 归零 / cmd107·108 覆盖 / 心跳 / 0x06 发现）**原样复用**。
主要变化是**作废一批 agent 时代对你提的请求**，外加请你在真机两车上**确认 3 件事**。

**为什么变**：DDO 软总线在「工业紫派 + 消费平板」这对硬件上凑不齐前提（蓝牙/超级终端/华为账号/DSched），
真机 `onlineDev=0` 打不通。**这是硬件限制、不是 DDO 不兼容**，所以平板侧 DDO 代码**保留**（日后换硬件可启用），
当前默认走更简单的「平板直连双车」。

---

## 一、保留不动（你已实现的，方案A 原样复用）

以下 handler **无需任何改动**，方案A 直接复用：

| 命令 | 你的动作 | 方案A 谁触发 |
|---|---|---|
| `105`/'i' distributed | `cmd124`：`wget http://<主机IP>:8000/defultMap.txt`+`roadFile.txt`（主机 IP 在 byte[1,2,4,6]） | **平板**直发给从车 |
| `5`/loadMap | 加载导航图(10) + **初始位姿归零 (0,0,0)** 到主车原点 | **平板**直发给从车（拉图后） |
| `107`/'k' + `108`/'l'(byte1=robotId) | 据对角矩形规划 FullRoad 覆盖(122)+分布式跟踪(123) | **平板**直发给每辆车 |
| 心跳（500ms，byte0=3 带位姿） | 回发到 `clientIP:5001` | 平板就是该车的 clientIP |
| `0x06` 广播发现 | 只回 9 字节、不记 client/不起心跳/不武装急停 | 平板扫描用 |
| `'m'`/0x6d forceCreateMap | LCM30+forceNewMap 清旧图重建 | 平板对主车发（建图） |

> Q13 的对齐（`eef8afe`：每辆车含 master 各领一矩形、统一 107/108、无主从特例）**继续有效，请勿回退**。

---

## 二、作废 / 撤回（agent 时代对你提的请求，方案A 下都不需要）

请**不必再做**以下事项（若已动手可停；若已做也无害，平板侧不依赖）：

1. **udp2lcm 心跳「回发到客户端源端口」** —— 原为「agent 绑 5002 与 udp2lcm 同机共存」而提
   （见 `contracts/app-purplepi-alignment-audit.md` 本机端口互斥 / `constants/protocol.ets` 注释）。
   方案A **车上不跑 agent**，平板直接在 **5001** 收发，你现状「心跳回 `clientIP:5001`」**正好对**。→ **此请求作废**。
2. **车载无界面 agent 的常驻支持**（Q6）：`BOOT_COMPLETED` 静态订阅白名单 / 预装 `system_basic` 系统应用 /
   常驻进程白名单 —— **都不需要**（车上不装 agent hap）。
3. **DISTRIBUTED_DATASYNC 预授权 / 软总线互信配置**（Q6/Q9）：**不需要**（不走 distributedDataObject）。
4. **方案A（整图走软总线）的 agent 文件写入能力**：搁置（本来就是后置增强）。

> 说明：平板侧的 DDO/软总线/互信代码**保留但默认不走**（硬件就绪后可一键切回）。所以上述能力是「日后若启用 DDO
> 再说」，**当前不阻塞**。

---

## 三、请你确认 / 真机验证（方案A 真正的新前提，仅 3 条）

方案A 的新变量是「**从车直接去主车 wget 拉图**」这条**车↔车**链路（以前默认也是方案B，但联调时多经平板）。请确认：

1. **主车 `:8000` 在自身跑 SLAM/导航时，能否稳定供从车 wget 拉图**？
   你的 `python -m http.server` 是单线程，RK3566 跑 SLAM 时实测 ~47KB/s（普通图 ~6.5MB 会很慢）。
   - 平板优先拉**压缩图 `zipedMap.txt`**（~6× 小），但**从车 cmd124 拉的是 `defultMap.txt`+`roadFile.txt`**（普通图）。
   - **请评估**：从车拉普通图的典型耗时？要不要让 cmd124 也优先拉 `zipedMap.txt`？或临时调高 http server 并发？
2. **cmd105→cmd124 拉图典型耗时**：平板发 cmd105 后**固定等 `SLAVE_PULL_DELAY_MS`（当前 8s）再发 cmd5**（协议无
   「拉完」回执）。**请给一个真机典型耗时**，好让我们把延时设够；**可选增强**：cmd124 完成后能否回一个信号
   （心跳保留字节 / 一个回包），让平板不靠死等。
3. **车↔车同网段 `:8000` 互访无隔离**：从车要能直连主车 IP:8000（不只是平板能连车）。换路由器/网段时，
   确认 AP 隔离 / 防火墙不挡车间 HTTP。

---

## 四、数据流（方案A 目标态）

```
            平板（中央协调器，一个 UDP socket）
   directUDP :5001 │                    │ directUDP :5001
        ┌──────────┘                    └──────────┐
   主车 Purple Pi 1                          从车 Purple Pi 2
   'm'→建图, cmd2→存图                        cmd105 ─┐(平板下发, 含主车IP)
   :8000 供 defultMap.txt ◀───── wget ────────────────┘ cmd124
   107/108(robotId0)→122/123                 cmd5→加载图+归零(0,0,0)
   心跳→平板                                  107/108(robotId1)→122/123
                                              心跳→平板
```

每辆车把平板当作它的**唯一** UDP 客户端（无 agent 竞争）——与你熟悉的单车手控完全同构，只是平板同时管两辆。

---

## 五、与现有契约的关系（便于你对照）

- `contracts/udp-protocol.md`：**命令布局/语义全不变**。cmd105 仍按旧布局 byte[1,2,4,6]（Q10 维持现状）。
- `contracts/multi-robot-collab.md`：其「车载 agent + 软总线黑板」拓扑**降级为"可选 DDO 路"**（保留、默认不走）。
  其中**方案B 车间拉图**正是方案A 实际用的链路。§坐标系/§定位约定（子机 cmd5 归零）**继续有效**。
- `contracts/integration-qa.md`：**Q6 / Q6.2 / A6.2「distributed 平板不直连任何车」约定反转**——方案A 下
  **平板直连每辆车**（每车单客户端=平板）。Q6 的 agent 常驻 / Q9 互信前提**作废**（见 §二）。

> 有疑问或上面 §三 任一条需要协议小改，请回写到 `contracts/integration-qa.md`，异步对齐即可。
