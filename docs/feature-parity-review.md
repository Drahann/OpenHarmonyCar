# 功能对等性复审：旧 App → 新 App（测试前回归排查）

> **背景（用户 2026-06-07）**：旧 App（`W:\CarApp\CarApp`，质量差但**能跑通整套流程**）重构成新 `app-harmony` 后，
> 须确保**没丢掉能跑通的功能**（明天测试要功能正常齐全）。本文彻底对照 **旧 App / 紫派底层接口（A 的
> 《接口功能与对接问题说明.md》）/ 新 App**，分两类：
> **A. 必补回归**（旧能跑、新丢了、影响功能 → 先施工）；**B. 新想法**（更优设计，需联合 A 开发，不塞进回归）。
> 流程：**先文档（本文）→ 后施工**。

## 0. 权威依据（A 文档 §四 + §六，逐条核对）

- **紫派 UDP `byte0` → 行为**：`0x00` 首包只建连+发 `7`(init)、**再发 0x00 才进入建图(LCM30, 0.05m)**；
  `0x01` 摇杆(wheel_ctrl)；`0x02` **停轮控+存图(32)+加载图(10)** = 结束建图；`0x03` 目标点(20)；
  `0x04` 取消导航(23)；`0x05` 加载图(10)+位姿归零(0,0,0)；`102/127` 全路径；`103/126` 取消全路径；
  `104/125` **选房间顶点**；`105/124` 子机拉主机图；`106` 加载图+目标；`107/108` 覆盖矩形对角点1/2(→122/123)。
- **🔴 关键事实**：**`Navi` 启动即删除 `defultMap.txt`** → 每次 Navi 运行**必须先建图才有地图**，没有"会话内预存图"。
  所以**不建图 → 无图 → HTTP 拉图永远拉不到 → 导航无图可走 → 全程卡死**。这就是你担心的"明天在建图上卡住"。

## 1. 命令覆盖对照（旧 App / 紫派要求 / 新 App）

| 操作 | UDP | 紫派行为(LCM) | 旧 App | 新 App | 结论 |
|---|---|---|---|---|---|
| 连接/init | 首包 | 建连+`7` | SetIP 点「连接」发 0 | 进页中性保活(pending+stop) | ✅ 对等 |
| **开始建图** | `0` | `30` 建图 | 「开始建图」按钮 | **无** | 🔴 **R1 必补** |
| 摇杆遥控 | `1` | wheel_ctrl | ↑↓←→ | 拖拽摇杆(上/左/右/停) | ✅ |
| **结束建图** | `2` | 存图`32`+加载`10` | 「结束建图」 | **无** | 🔴 **R1 必补** |
| 选点导航 | `3` | 目标`20` | 选点+导航 | astar 选点+开始导航 | ✅ |
| 取消导航 | `4` | `23` | 取消 | 取消 | ✅ |
| 加载已有图 | `5` | `10`+归零 | (重新建图相关) | ControlPage 无（**car-agent 有**） | 🟡 R3（单车被建图覆盖） |
| 全路径启动 | `102` | `127` | 全路径 | 开始覆盖 | ✅ |
| 全路径取消 | `103` | `126` | | 停止 | ✅ |
| **全路径选房间** | `104` | `125` | 选房间顶点 | **无** | 🟠 **R2（fullpath 不完整）** |
| 子机拉主机图 | `105` | `124` | 分布式 | ControlPage 无（**car-agent 有**） | 🟡 多机/agent 场景 |
| 分布式目标 | `106` | `10`+`20` | | ControlPage 无（agent 有） | 🟡 agent 场景 |
| 覆盖矩形 | `107/108` | `122/123` | 划区 | 两点划区 | ✅ |
| 多机设备发现 | 软总线 | — | 查找设备/平板 | DeviceList+黑板+agent | 🟡 架构不同（非单车阻塞） |
| 隐藏/显示摇杆 | — | — | 隐藏/显示 | 固定浮层 | 🟢 R4（小 UX） |

## 2. A. 必补回归（影响功能，先施工）

### 🔴 R1 建图 `cmd0` + `cmd2` —— 最高优先，明天测试的硬阻塞
- **为什么硬阻塞**：Navi 启动删图 → 不建图就没有任何地图 → 拉图/导航全废。
- **旧 App**：`Navigationcomponents`(PendingComponent) 有「开始建图」(cmd0)/「结束建图」(cmd2)/「重新建图」。
- **新 App**：`ControlPage` 没有这组（我当初为"进页不误触建图"去掉了 cmd0，连入口一起丢了）。
- **已补（commit f014768）**：`ControlPage` 建图卡——地图未就绪时显示：「开始建图」(cmd0) → 摇杆驱动探索 →
  「结束建图」(cmd2) → 触发拉图 → 地图就绪后转正常操作。

### 🟠→✅ R2 全路径**选算法 + 选 4 顶点**（cmd102 algNum + cmd104）—— 已补
> ⚠️ **二次复审更正**：首轮我写成"只缺选房间"，**漏了「选算法」**（用户指出）。说明首轮扫描不够全 —— 完整流程比我以为的多一步。
- **旧 App**（`Navigationcomponents`）：① 选算法「牛耕算法 / 最小生成树」(`FullPathAlgorithmFlags`) → `cmd102` byte1=algNum(0/1)；
  ② 点地图选 **4 个顶点**（左下→右下→右上→左上，`cmd104` byte1=序号），满 4 个后 1s 自动 `cmd102` 启动。
- **新 App 原状**：只有 开始覆盖(cmd102,algNum=0)/停止，**既无选算法也无选顶点** → fullpath 跑不对。
- **已补（commit ad68c72）**：FullpathOps = 算法 chip(牛耕/最小生成树) + 点地图选 4 顶点(cmd104×4) → 满 4 自动 cmd102(algNum) + 重选顶点 + 停止。

### ✅ S1 摇杆速度控制（用户要求）
- **旧 App**：摇杆速度按 mode 写死（SPEED_*），无用户控制。
- **已补（commit ad68c72）**：ControlPage 加速度滑块(10–100%)，`@State joySpeed` 经 Joystick 下发 `cmd1` 的 speed；进页给 mode 默认值、用户可调。

### 🟡 R3 加载已有图 `cmd5`（次要，单车被建图覆盖）
- 单车每会话都现建图(R1)，故 ControlPage 暂不强依赖 cmd5；**car-agent 已用 cmd5**（子机归零）。
  保留观察：若出现"已有图免重建"需求再给 ControlPage 加「加载地图」。

### 🟢 R4 隐藏/显示摇杆（小 UX）
- 旧可隐藏摇杆腾出地图；新为固定浮层。低优先，可加收起按钮。

## 3. B. 新想法（联合 A 开发；不塞进单车回归）

### 💡 N1 实时建图预览（你提的）
- **现状**：新 App 只在**建完图后** HTTP 拉**整张** `defultMap.txt`。
- **底层已有**：紫派有 LCM `MAPFILE`(地图**分片**) + `SERVICE_COMMAND`(建图/保存**状态**)。
- **想法**：建图时把地图**增量**推到平板，**边建边看地图被构建**。
- **需 A 配合**：把 `MAPFILE`/部分地图经 **UDP 新命令或 HTTP 增量端点**暴露给 App（C/C++ 栈现只 HTTP 整图）。→ 写 `integration-qa` **Q7**。

### 💡 N2 可靠的"地图就绪"信号（取代 App 猜文件 ≥ 3.24MB）
- **现状**：新 App `pollMapUntilReady` 靠"文件 ≥ `MAP_READY_MIN_BYTES`(324e4)"判就绪 —— **脆**（真图更小则永不就绪）。
- **想法**：用紫派 `SERVICE_COMMAND` 的"建图完成/已存图"状态回传给 App 当就绪信号。→ `integration-qa` **Q8**。
  （短期 R1 落地后，可在 `cmd2 结束建图`后**主动拉一次**图，先绕开阈值脆性。）

## 4. 明天单车测试 · 最小可用流程（补完 R1 后）

```
发现/连接车 → 【开始建图(R1)】 → 摇杆驱动小车探索一圈 → 【结束建图(R1)】
            → (自动拉图并显示) → 点地图选目标点 → 【开始导航】 → (到点) ；随时可摇杆遥控
```
全路径模式额外需 R2（选房间）。多机/分布式走 agent + 黑板（非单车测试范围）。

## 5. 施工顺序

1. **R1 建图**（ControlPage 建图卡 + cmd0/cmd2 + cmd2 后拉图）—— 先做、先验证（mock-purplepi/mock-app）。
2. **R2 全路径选房间**（cmd104）。
3. 回写 `operation-guide.md`（建图步骤）。
4. **N1/N2 写进 `contracts/integration-qa.md`（Q7/Q8）**与 A 异步沟通；待 A 后再联合开发。
5. R3/R4 视情况补。

## 6. 分布式（多机协同）逻辑复审 —— 明天测试会不会出问题？

**结论：单车直发可测、不会崩；真·多机协同今天不具备（agent 未部署 + App 未写黑板），别期待车间协同。**

**6.1 新 App 现状（核对 `ControlPage`）**
- distributed 模式选点：连点 2 个对角点 → `sendDistArea(c1,c2,0)` = `cmd107`(对角点1) + `cmd108`(对角点2,robotId=0)，**直发 `this.ip`（单台连上的车），不经软总线黑板**。
- `ControlPage` **从不调用** `FleetMissionService.publishMission/joinSession` —— 黑板只在 `EntryAbility` `init`（建 dataObject、不入会），**App 侧根本没往黑板写 assignments**。
- 紫派侧（A 文档）：`108` → 加载本机地图 + 据两对角点发 `122` 生成矩形覆盖 + `123` 跟踪。

**6.2 明天单车测试：✅ 能跑（与旧 App 等价的直发路径）**
- 流程：建图(R1) → 切「多机协同」mode → 地图上点 2 个对角点 → 该车直接开始矩形覆盖。**不依赖 agent/软总线。**
- 已修：`107→108` 与 `4 顶点→102` 加了**命令间延时**（`CMD_SEQ_GAP_MS`/`FULLPATH_START_DELAY_MS`），对照旧 App，避免"暂存后使用"的 UDP 乱序/未处理完。
- 不会崩：`FleetMissionService.init` 无可信对端时只是没 peer，不报错；distributed UI（划矩形/进度条）正常。

**6.3 真·多机协同（黑板链路）：App 侧已接通，待 agent 部署 + 软总线互信**
- ✅ **App 已接黑板（commit 63dc165）**：distributed 模式 `ControlPage` 入会 `FLEET_SESSION_ID` + 划矩形→`assignArea`→默认
  写黑板（`publishMission`，`phase=covering` + `Assignment(carId,robotId)`）让各车 agent 读自己 carId 那块；订阅黑板取各车回写位姿更新地图；
  可切「平板直发兜底」(`distUseAgent`)。agent 端本机命令也加了 300ms 错开。
- ⏳ **还差两件（owner 明天处理）**：① 把 **car-agent 装上紫派**（owner 明天装）；② **软总线设备互信**——平板与紫派 OH
  要在可信分布式网络里（`DISTRIBUTED_DATASYNC` 授权 + 设备配对/互信），`distributedDataObject` 才会同步（**Q6.3，属设备配置、非代码**）。
- **明天能否测多机**：装了 agent + 互信配好 → "平板划区→黑板→agent→车覆盖→位姿回平板"闭环可测（即便单车也能验证整条链路）；
  **互信没配好就同步不了** → 临时把开关切到「平板直发兜底」先验证覆盖本身。

**6.4 注意项（不阻塞，记着）**
- **进度条恒 0%**：心跳不带覆盖进度，单车覆盖时进度条不动（覆盖照跑）。真进度需软总线（agent 回写）或 A 在心跳留进度字节（≈Q8）。
- **`robotId=0` 写死**：单车覆盖发 `cmd108` byte1=0。**待 A 确认**：单车独立覆盖 robotId=0 是否正确，还是要匹配该车配置 id（多机时 0/1 区分主从）。→ 记 integration-qa。
- **连多台只控主车**：`sendDistArea` 只发 `this.ip`；DeviceList 连了第 2 台也收不到覆盖命令（多机分区靠 6.3 的黑板链路，未通）。
