# 统一行尾，避免 Windows 团队的 CRLF/LF 之争（仓库内一律 LF）
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.bat   text eol=crlf
*.ps1   text eol=crlf

# 明确的二进制类型（不做行尾转换）
*.png   binary
*.jpg   binary
*.jpeg  binary
*.svg   text
*.pdf   binary
*.hap   binary

# ===== 可选：用 Git LFS 版本化模型权重 / 大文件 =====
# 默认这些类型在 .gitignore 里被忽略。若要纳入版本管理：
#   1) 安装并启用： git lfs install
#   2) 把对应类型从 .gitignore 移除
#   3) 取消下面几行的注释
# *.om     filter=lfs diff=lfs merge=lfs -text
# *.pt     filter=lfs diff=lfs merge=lfs -text
# *.onnx   filter=lfs diff=lfs merge=lfs -text
# *.mindir filter=lfs diff=lfs merge=lfs -text
# *.mp4    filter=lfs diff=lfs merge=lfs -text
